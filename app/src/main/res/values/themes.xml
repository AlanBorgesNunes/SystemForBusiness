<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SystemForBusiness" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/seven</item>
        <item name="colorPrimaryVariant">@color/five</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/forr</item>
        <item name="colorSecondaryVariant">@color/three</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.SystemForBusiness" parent="Base.Theme.SystemForBusiness"/>

    <style name="SpinnerStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">8dp</item>
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:dropDownSelector">@android:color/darker_gray</item>
        <item name="android:popupBackground">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:popupElevation">8dp</item>
        <item name="android:popupAnimationStyle">@style/SpinnerDropdownAnimation</item>
    </style>

    <style name="SpinnerDropdownAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>